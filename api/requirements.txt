# Runtime saja, setiap baris menambah ukuran image.
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.7,<3
httpx>=0.27,<1
numpy<2
tensorflow-cpu==2.15.1

# google-generativeai sengaja TIDAK dipakai. Gemini dipanggil lewat REST
# dengan httpx yang sudah ada -- satu dependensi berat lebih sedikit di
# image, dan kontrol timeout langsung di tangan kita.
