// STUB milik Agent B — Agent F menimpanya.
//
// Berkas ini hanya ada supaya router bisa dikompilasi dan tiga akun demo bisa
// dibuktikan mendarat di shell yang benar. Jangan menambahkan logika di sini:
// tulis ulang seluruh isinya.
import 'package:flutter/material.dart';

class PartnerRiwayatScreen extends StatelessWidget {
  const PartnerRiwayatScreen({super.key});

  @override
  Widget build(BuildContext context) => Center(
    child: Text('Pengepul · Riwayat', style: Theme.of(context).textTheme.headlineMedium),
  );
}
